# Automated-File-Deletion
This Python script's main function is to identify a folder path a 'delete' files stored that are older than six months. Files deleted would go to Recycle Bin using the send2trash module as a fallback method to avoid accidentally deleting important files permanently.
